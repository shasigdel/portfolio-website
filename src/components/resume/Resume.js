import React from 'react';
import resume from '../../assets/resume/resume.pdf';

const Resume = () => {
  return (
    <section id="resume">
      <a href={resume}>
      </a>
    </section>
  );
}; 

export default Resume;
